apiVersion: v1
kind: Service
metadata:
  name: overprovisioned-api
  namespace: kubefit-demo
spec:
  selector:
    app: overprovisioned-api
  ports:
    - name: http
      port: 80
      targetPort: 80
